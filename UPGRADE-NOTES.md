# 정보 실습 워크북 — 업그레이드 변경 내역 (교실 활용 보강)

## script.js
- 카드에 role="button" tabindex="0" aria-label 부여 → 키보드/스크린리더로 차시 선택 가능
- 확인문제 보기(.opt), 오류찾기 줄(.bugline), AI 샘플(.sample)에 role/tabindex 부여
- 전역 keydown 핸들러 추가: div로 만든 조작 요소를 Enter·Space로 실행(스크롤 방지 포함)
- 피드백 영역에 role="status" aria-live="polite" → 정답/오답 결과를 스크린리더가 읽어 줌
- 잘못된 링크(#없는차시)·renderLesson(잘못된 id) 시 화면이 멈추지 않고 "차시를 찾을 수 없어요" 안내 표시(방어 코드)
- AREAS[l.area] 미존재 시에도 안전하게 동작하도록 기본값 처리
- 오답 안내 문구를 "초록색이 정답" → "✓ 표시된 항목이 정답"으로 변경(색 의존 제거)
- 홈 인공지능 영역에 이론·교사용 사이트(info-ai) 바로가기 링크 추가

## styles.css
- 키보드 포커스 표시(:focus-visible 파란 외곽선) 전 조작 요소에 추가, 입력창 focus 표시 복원
- 정답/오답을 기호(✓/✕)로도 표시하는 ::after 규칙 추가(색각 이상 배려)
- 태블릿(≤820px)·모바일(≤560px) 반응형 보강: 내비 꽉 채움, 카드 1열, 차시이동/저장바 세로 배치, 잠금 폼 세로 배치 등
- AI 바로가기 링크(.area-link) 스타일

## 추가 기능 (인공지능 차시 답 쓰기)
- 인공지능 8개 차시(a01~a08)에 "✍️ 내 답·기록 쓰기" 섹션 추가 — 실습 관찰·판단을 직접 적는 서술형 입력칸(자동저장)
  - a04·a08은 3칸, 나머지는 2칸 (차시 내용에 맞춘 질문)
- script.js: record 섹션 렌더링 지원 추가(따라하기·바꿔보기 뒤, 정리 앞에 배치)
- styles.css: .record-item / .record-q 스타일 추가
- 프로그래밍·피지컬 차시는 실습 중심이라 기록칸을 넣지 않음(요청 취지 반영)
